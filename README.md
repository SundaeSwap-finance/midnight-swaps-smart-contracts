# Midnight Intents Smart Contracts

Contract source code. Precompiled so that we can use keys/ZKIR from older versions if needed.

## Setup

This is mostly intended to be used as a submodule.

If you've updated contracts, run `npm run compile` to (confirm your changes compile and) update precompiled files.
